# k8s-upgrade-assess dependencies
# Python 3.10+ required

# Phase 2 — Embeddings + Vector DB
faiss-cpu>=1.7.4
sentence-transformers>=2.7.0

# Phase 1 — Web scraping / doc collection
requests>=2.31.0
beautifulsoup4>=4.12.0

# Transitive (pinned for reproducibility)
numpy>=1.24.0
